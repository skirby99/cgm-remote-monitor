# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - ashley-drip

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read #This is required for actions/checkout

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to container registry
        uses: docker/login-action@v2
        with:
          registry: nightscout/
          username: ${{ secrets.AZUREAPPSERVICE_CONTAINERUSERNAME_7FBA69C219A54D73A8F9204CF04342F7 }}
          password: ${{ secrets.AZUREAPPSERVICE_CONTAINERPASSWORD_56240787507E435C811E5770C9B0FD92 }}

      - name: Build and push container image to registry
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true
          tags: nightscout/${{ secrets.AZUREAPPSERVICE_CONTAINERUSERNAME_7FBA69C219A54D73A8F9204CF04342F7 }}/cgm-remote-monitor:${{ github.sha }}
          file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    
    needs: build
    
    steps:
      
      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'ashley-drip'
          slot-name: 'Production'
          images: 'nightscout/${{ secrets.AZUREAPPSERVICE_CONTAINERUSERNAME_7FBA69C219A54D73A8F9204CF04342F7 }}/cgm-remote-monitor:${{ github.sha }}'
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_DF737616034C493BB9718822B2B872AF }}